import { Metadata } from 'next'
import { FormAdd } from '../partials/form'

export const metadata: Metadata = {
  title: 'Add Category',
  description: 'Add Category - House of Wizard',
  robots: {
    index: false,
    follow: false,
  },
}

export default FormAdd
