import { Metadata } from 'next'
import { FormAdd } from '../form'

export const metadata: Metadata = {
  title: 'Add Role Permission',
  description: 'Add Role Permission - House of Wizard',
  robots: {
    index: false,
    follow: false,
  },
}

export default FormAdd
