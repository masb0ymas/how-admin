import { Metadata } from 'next'
import { FormAdd } from '../partials/form'

export const metadata: Metadata = {
  title: 'Add Private Membership',
  description: 'Add Private Membership - House of Wizard',
  robots: {
    index: false,
    follow: false,
  },
}

export default FormAdd
